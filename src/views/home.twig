{% extends 'layouts/public.twig' %}
{% block title %}Accueil{% endblock%}

{% block content %} 
<link rel="stylesheet" href="/css/main.css">
<div id="home">
    
    <h1 class="title-main">À la une </h1>

    <div id= "movies">
        {% for movie in movies %}
            <article>
                <a href="{{ getUrl('details' , {'slug': movie.slug}) }}">
                    <img src="image/posters/{{ movie.photo }}" alt="{{ movie.title}}">
                    <h2>{{ movie.title}}</h2>
                </a>
            </article>
        {% endfor %} 
    </div>
</div>
{% endblock %}
